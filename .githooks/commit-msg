#!/usr/bin/env bash

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
message_file="$1"
subject="$(head -n 1 "$message_file")"

"$repo_root/.github/scripts/validate-conventional-commit.sh" "$subject"
